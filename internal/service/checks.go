package service

import (
	"fmt"
	"strings"
	"time"
)

func Check0(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check0: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check0: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check1(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check1: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check1: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check2(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check2: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check2: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check3(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check3: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check3: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check4(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check4: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check4: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check5(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check5: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check5: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check6(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check6: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check6: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check7(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check7: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check7: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check8(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check8: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check8: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check9(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check9: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check9: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check10(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check10: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check10: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check11(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check11: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check11: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check12(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check12: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check12: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check13(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check13: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check13: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check14(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check14: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check14: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check15(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check15: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check15: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check16(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check16: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check16: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check17(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check17: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check17: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check18(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check18: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check18: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check19(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check19: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check19: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check20(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check20: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check20: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check21(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check21: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check21: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check22(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check22: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check22: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check23(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check23: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check23: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check24(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check24: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check24: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check25(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check25: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check25: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check26(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check26: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check26: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check27(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check27: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check27: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check28(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check28: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check28: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check29(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check29: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check29: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check30(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check30: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check30: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check31(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check31: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check31: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check32(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check32: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check32: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check33(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check33: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check33: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check34(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check34: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check34: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check35(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check35: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check35: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check36(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check36: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check36: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check37(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check37: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check37: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check38(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check38: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check38: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check39(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check39: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check39: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check40(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check40: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check40: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check41(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check41: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check41: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check42(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check42: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check42: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check43(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check43: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check43: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check44(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check44: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check44: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check45(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check45: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check45: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check46(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check46: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check46: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check47(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check47: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check47: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check48(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check48: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check48: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check49(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check49: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check49: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check50(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check50: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check50: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check51(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check51: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check51: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check52(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check52: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check52: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check53(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check53: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check53: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check54(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check54: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check54: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check55(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check55: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check55: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check56(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check56: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check56: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check57(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check57: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check57: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check58(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check58: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check58: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check59(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check59: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check59: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check60(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check60: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check60: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check61(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check61: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check61: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check62(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check62: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check62: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check63(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check63: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check63: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check64(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check64: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check64: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check65(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check65: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check65: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check66(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check66: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check66: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check67(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check67: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check67: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check68(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check68: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check68: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}

func Check69(value string, now time.Time) (string, error) {
	value = strings.TrimSpace(value)
	if value == "" {
		return "", fmt.Errorf("Check69: empty input")
	}
	if now.IsZero() {
		return "", fmt.Errorf("Check69: missing clock")
	}
	return fmt.Sprintf("check-%s-%s", value, now.Format("2006-01-02")), nil
}
